# Copy this file to .env.test.local (gitignored) and fill in your real
# Stripe test-mode secret key to run charge-real-stripe.test.ts against
# Stripe's actual API. Every other test uses a mocked stripe client and
# doesn't need this.
STRIPE_KEY=sk_test_...
